#!/bin/bash

# omarchy:summary=Update and ensure all default BLARCHY packages are installed

set -euo pipefail

# Keep package aliases, AUR handling, and system integration in one place.
# The standalone installer is idempotent and prompts for sudo only where needed.
exec "$OMARCHY_PATH/install.sh"
